# AromaSouQ Platform - Architecture Diagram

## 📐 Complete System Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        CLIENT LAYER                                      │
│                                                                          │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐             │
│  │   Web App    │    │   iOS App    │    │  Android App │             │
│  │  (Next.js)   │    │   (Swift)    │    │   (Kotlin)   │             │
│  │              │    │              │    │              │             │
│  │  - Homepage  │    │  - Native UI │    │  - Native UI │             │
│  │  - Products  │    │  - Camera    │    │  - Camera    │             │
│  │  - Checkout  │    │  - Push      │    │  - Push      │             │
│  │  - Dashboard │    │  - Wallet    │    │  - Wallet    │             │
│  └──────┬───────┘    └──────┬───────┘    └──────┬───────┘             │
│         │                   │                   │                      │
└─────────┼───────────────────┼───────────────────┼──────────────────────┘
          │                   │                   │
          └───────────────────┴───────────────────┘
                              │
┌─────────────────────────────┼────────────────────────────────────────────┐
│                      API GATEWAY (Port 8000)                             │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────┐    │
│  │  Kong API Gateway                                               │    │
│  │  - Authentication (JWT validation)                              │    │
│  │  - Rate limiting (100 req/min per user)                        │    │
│  │  - Load balancing (round-robin)                                │    │
│  │  - Request logging & analytics                                 │    │
│  │  - CORS handling                                               │    │
│  └────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────┬────────────────────────────────────────────┘
                              │
┌─────────────────────────────┴────────────────────────────────────────────┐
│                      MICROSERVICES LAYER                                 │
│                                                                          │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐     │
│  │  User Service    │  │ Product Service  │  │  Order Service   │     │
│  │  (NestJS)        │  │ (NestJS)         │  │  (NestJS)        │     │
│  │  Port: 3100      │  │ Port: 3101       │  │  Port: 3102      │     │
│  │                  │  │                  │  │                  │     │
│  │ - Auth           │  │ - CRUD           │  │ - Cart           │     │
│  │ - Profile        │  │ - Search         │  │ - Checkout       │     │
│  │ - Wallet         │  │ - Reviews        │  │ - Tracking       │     │
│  │ - Addresses      │  │ - Categories     │  │ - Returns        │     │
│  └────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘     │
│           │                     │                     │                │
│  ┌────────┴─────────┐  ┌────────┴─────────┐  ┌────────┴─────────┐     │
│  │ Payment Service  │  │ Vendor Service   │  │ Delivery Service │     │
│  │ (NestJS)         │  │ (NestJS)         │  │ (NestJS)         │     │
│  │ Port: 3103       │  │ Port: 3104       │  │ Port: 3105       │     │
│  │                  │  │                  │  │                  │     │
│  │ - Stripe         │  │ - Dashboard      │  │ - Fetchr         │     │
│  │ - Telr           │  │ - Products       │  │ - Aramex         │     │
│  │ - PayTabs        │  │ - Orders         │  │ - SMSA           │     │
│  │ - Refunds        │  │ - Earnings       │  │ - DHL            │     │
│  └────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘     │
│           │                     │                     │                │
│  ┌────────┴─────────┐  ┌────────┴─────────┐  ┌────────┴─────────┐     │
│  │  Chat Service    │  │ Notification     │  │  AI Service      │     │
│  │  (NestJS)        │  │ Service (NestJS) │  │  (Python)        │     │
│  │  Port: 3106      │  │ Port: 3107       │  │  Port: 8080      │     │
│  │                  │  │                  │  │                  │     │
│  │ - Socket.io      │  │ - Push (FCM)     │  │ - Scent Match    │     │
│  │ - AI Bot         │  │ - SMS (Twilio)   │  │ - Recommendations│     │
│  │ - Video (Agora)  │  │ - Email (SG)     │  │ - PyTorch/TF     │     │
│  │ - History        │  │ - WhatsApp       │  │ - Qdrant         │     │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘     │
│                                                                         │
└─────────────────────────────┬───────────────────────────────────────────┘
                              │
┌─────────────────────────────┴────────────────────────────────────────────┐
│                         DATA LAYER                                       │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  PostgreSQL (Port 5432)                                           │   │
│  │  - users, addresses                                               │   │
│  │  - orders, order_items, payments                                  │   │
│  │  - vendors, vendor_documents, vendor_earnings                     │   │
│  │  - influencers, affiliate_links, affiliate_clicks                │   │
│  │  - wallet_transactions, saved_cards                              │   │
│  │  - shipments, delivery_tracking                                  │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  MongoDB (Port 27017)                                             │   │
│  │  - products (with 10-category taxonomy)                           │   │
│  │  - categories, brands                                             │   │
│  │  - reviews                                                        │   │
│  │  - chat_conversations, chat_messages                              │   │
│  │  - user_activity (analytics events)                               │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  Redis (Port 6379)                                                │   │
│  │  - Session store (user sessions)                                  │   │
│  │  - API cache (product pages, 5min TTL)                           │   │
│  │  - Rate limiting counters                                         │   │
│  │  - Real-time inventory counts                                     │   │
│  │  - Pub/Sub (real-time updates)                                   │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  Elasticsearch (Port 9200)                                        │   │
│  │  - products index (full-text search)                              │   │
│  │  - vendors index                                                  │   │
│  │  - Faceted search (15+ filters)                                   │   │
│  │  - Auto-suggestions                                               │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  Qdrant (Ports 6333, 6334)                                        │   │
│  │  - product_images collection (512-dim vectors)                    │   │
│  │  - scent_profiles collection (256-dim vectors)                    │   │
│  │  - user_preferences collection                                    │   │
│  │  - Cosine similarity search (<50ms)                              │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  Kafka (Port 9092)                                                │   │
│  │  Topics:                                                          │   │
│  │  - order.created, order.updated                                   │   │
│  │  - payment.completed                                              │   │
│  │  - inventory.updated                                              │   │
│  │  - user.registered                                                │   │
│  │  - delivery.status                                                │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │  MinIO / AWS S3                                                   │   │
│  │  - Product images (multiple sizes)                                │   │
│  │  - Videos (UGC, brand content)                                    │   │
│  │  - User uploads (for Scent Match AI)                             │   │
│  │  - Documents (vendor KYC, invoices)                              │   │
│  │  CDN: CloudFront (global delivery)                               │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────┴────────────────────────────────────────────┐
│                   EXTERNAL INTEGRATIONS                                  │
│                                                                          │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐            │
│  │ Payment        │  │ Delivery       │  │ Communication  │            │
│  │ Gateways       │  │ Partners       │  │ Services       │            │
│  │                │  │                │  │                │            │
│  │ • Stripe       │  │ • Fetchr (UAE) │  │ • Twilio       │            │
│  │ • Telr (GCC)   │  │ • Aramex (GCC) │  │   - SMS        │            │
│  │ • PayTabs      │  │ • SMSA (Saudi) │  │   - WhatsApp   │            │
│  │                │  │ • DHL (Intl)   │  │                │            │
│  │                │  │ • Custom       │  │ • SendGrid     │            │
│  │                │  │                │  │   - Email      │            │
│  └────────────────┘  └────────────────┘  └────────────────┘            │
│                                                                          │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐            │
│  │ Real-Time      │  │ AI/ML          │  │ Analytics      │            │
│  │ Services       │  │ Services       │  │ & Monitoring   │            │
│  │                │  │                │  │                │            │
│  │ • Agora        │  │ • OpenAI       │  │ • Sentry       │            │
│  │   - Video      │  │   - GPT-4 Bot  │  │ • Mixpanel     │            │
│  │   - Voice      │  │                │  │ • Google       │            │
│  │                │  │ • Custom ML    │  │   Analytics    │            │
│  │ • Pusher       │  │   - Scent      │  │                │            │
│  │   - Realtime   │  │     Match      │  │ • Prometheus   │            │
│  └────────────────┘  └────────────────┘  └────────────────┘            │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow Diagrams

### 1. User Registration & Login Flow

```
┌────────────┐
│   Client   │
│  (Web/App) │
└─────┬──────┘
      │
      │ POST /auth/register
      │ { email, password, firstName, lastName }
      ▼
┌─────────────────┐
│  API Gateway    │
│  - Validate     │
│  - Rate limit   │
└─────┬───────────┘
      │
      │ Forward request
      ▼
┌─────────────────┐        ┌──────────────┐
│  User Service   │───────▶│  PostgreSQL  │
│  - Hash password├────────│  INSERT user │
│  - Create user  │◀───────│  RETURN id   │
└─────┬───────────┘        └──────────────┘
      │
      │ Generate JWT
      │
      ├──────────────────┐
      │                  │
      ▼                  ▼
┌──────────────┐   ┌─────────────────┐
│  SendGrid    │   │  Kafka          │
│  Send        │   │  Emit event:    │
│  welcome     │   │  user.registered│
│  email       │   │                 │
└──────────────┘   └─────────────────┘
      │
      │ Return tokens
      ▼
┌────────────┐
│   Client   │
│  Store JWT │
└────────────┘
```

### 2. Product Search Flow

```
┌────────────┐
│   Client   │
└─────┬──────┘
      │
      │ GET /products/search?q=oud&type=original&price_max=500
      ▼
┌─────────────────┐
│  API Gateway    │
└─────┬───────────┘
      │
      ▼
┌─────────────────────┐        ┌──────────────────┐
│  Product Service    │───────▶│  Redis           │
│  - Check cache      │◀───────│  Check cache key │
└─────┬───────────────┘        └──────────────────┘
      │
      │ Cache MISS
      ▼
┌──────────────────────┐       ┌──────────────────┐
│  Elasticsearch       │◀──────│  Product Service │
│  - Full-text search  │       │  - Build query   │
│  - Faceted filters   │       │  - Add filters   │
│  - Sort & paginate   │       │                  │
└─────┬────────────────┘       └──────────────────┘
      │
      │ Return product IDs
      ▼
┌──────────────────────┐       ┌──────────────────┐
│  MongoDB             │◀──────│  Product Service │
│  - Get full product  │       │  - Fetch details │
│    documents by IDs  │       │  - Enrich data   │
└─────┬────────────────┘       └──────────────────┘
      │
      │ Return products
      ▼
┌──────────────────────┐       ┌──────────────────┐
│  Product Service     │──────▶│  Redis           │
│  - Cache result      │       │  SET key (5min)  │
└─────┬────────────────┘       └──────────────────┘
      │
      │ Return JSON
      ▼
┌────────────┐
│   Client   │
│  Display   │
│  products  │
└────────────┘
```

### 3. Order & Payment Flow

```
┌────────────┐
│   Client   │
└─────┬──────┘
      │
      │ POST /orders
      │ { items: [...], addressId, paymentMethod: 'stripe' }
      ▼
┌─────────────────┐
│  Order Service  │
└─────┬───────────┘
      │
      │ 1. Validate cart
      ├──────────────────┐
      │                  │
      ▼                  ▼
┌──────────────┐   ┌─────────────┐
│  Product     │   │  Inventory  │
│  Service     │   │  Service    │
│  Check       │   │  Reserve    │
│  availability│   │  stock      │
└──────────────┘   └─────────────┘
      │
      │ 2. Calculate pricing
      │    (subtotal, shipping, tax, cashback)
      │
      ▼
┌─────────────────┐        ┌──────────────┐
│  Order Service  │───────▶│  PostgreSQL  │
│  Create order   │◀───────│  INSERT      │
└─────┬───────────┘        └──────────────┘
      │
      │ 3. Process payment
      ▼
┌──────────────────┐       ┌──────────────┐
│  Payment Service │──────▶│  Stripe API  │
│  Create intent   │◀──────│  Return      │
│                  │       │  client      │
│                  │       │  secret      │
└─────┬────────────┘       └──────────────┘
      │
      │ Return payment intent
      ▼
┌────────────┐
│   Client   │
│  Stripe    │
│  Elements  │
│  Complete  │
│  payment   │
└─────┬──────┘
      │
      │ Payment success webhook
      ▼
┌──────────────────┐       ┌──────────────┐
│  Payment Service │──────▶│  PostgreSQL  │
│  Update payment  │       │  UPDATE      │
│  status          │       │  payment     │
└─────┬────────────┘       └──────────────┘
      │
      │ Emit event
      ▼
┌─────────────────┐
│  Kafka          │
│  payment.       │
│  completed      │
└─────┬───────────┘
      │
      ├────────────┬─────────────┬──────────────┐
      │            │             │              │
      ▼            ▼             ▼              ▼
┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│ Order    │ │ Delivery │ │ Wallet   │ │ Notif.   │
│ Service  │ │ Service  │ │ Service  │ │ Service  │
│ Confirm  │ │ Create   │ │ Credit   │ │ Send     │
│ order    │ │ shipment │ │ cashback │ │ email/SMS│
└──────────┘ └──────────┘ └──────────┘ └──────────┘
```

### 4. AI Scent Match Flow

```
┌────────────┐
│   Client   │
│  (Mobile)  │
└─────┬──────┘
      │
      │ Take photo of perfume bottle
      │
      │ POST /ai/scent-match
      │ { image: base64 }
      ▼
┌─────────────────┐
│  API Gateway    │
└─────┬───────────┘
      │
      ▼
┌─────────────────────┐       ┌──────────────────┐
│  AI Service         │──────▶│  MinIO/S3        │
│  (Python FastAPI)   │       │  Upload image    │
└─────┬───────────────┘       └──────────────────┘
      │
      │ 1. Preprocess image
      │    (resize, normalize)
      │
      ▼
┌─────────────────────┐       ┌──────────────────┐
│  CLIP Model         │◀──────│  AI Service      │
│  Generate 512-dim   │       │  Load model      │
│  embedding vector   │       │                  │
└─────┬───────────────┘       └──────────────────┘
      │
      │ embedding: [0.123, -0.456, ...]
      ▼
┌─────────────────────┐       ┌──────────────────┐
│  Qdrant Vector DB   │◀──────│  AI Service      │
│  Similarity search  │       │  Query top 20    │
│  Cosine distance    │       │  similar vectors │
│  < 50ms             │       │                  │
└─────┬───────────────┘       └──────────────────┘
      │
      │ Return product IDs with similarity scores
      ▼
┌─────────────────────┐       ┌──────────────────┐
│  Product Service    │◀──────│  AI Service      │
│  Get full product   │       │  Fetch details   │
│  details            │       │  for IDs         │
└─────┬───────────────┘       └──────────────────┘
      │
      │ Rank by similarity + business rules
      │ (prioritize "Our Brand", in-stock, etc.)
      ▼
┌────────────┐
│   Client   │
│  Display   │
│  "Similar  │
│  Products" │
│  list      │
└────────────┘
```

### 5. Delivery Tracking Flow

```
┌────────────┐
│  Vendor    │
│  Dashboard │
└─────┬──────┘
      │
      │ POST /shipments
      │ { orderId, partnerId: 'fetchr' }
      ▼
┌──────────────────┐       ┌──────────────┐
│ Delivery Service │──────▶│ PostgreSQL   │
│ Create shipment  │       │ INSERT       │
└─────┬────────────┘       └──────────────┘
      │
      │ Call partner API
      ▼
┌──────────────────┐       ┌──────────────┐
│ Fetchr API       │◀──────│ Delivery     │
│ Create shipment  │       │ Service      │
│ Return tracking  │       │              │
│ number & label   │       │              │
└─────┬────────────┘       └──────────────┘
      │
      │ tracking_number: FE123456789
      │ label_url: https://...
      ▼
┌──────────────────┐       ┌──────────────┐
│ Delivery Service │──────▶│ PostgreSQL   │
│ Update shipment  │       │ UPDATE       │
└─────┬────────────┘       └──────────────┘
      │
      │ Emit event
      ▼
┌─────────────────┐
│  Kafka          │
│  shipment.      │
│  created        │
└─────┬───────────┘
      │
      ├────────────┬──────────────┐
      │            │              │
      ▼            ▼              ▼
┌──────────┐ ┌──────────┐ ┌──────────┐
│ Order    │ │ Notif.   │ │ Vendor   │
│ Service  │ │ Service  │ │ Service  │
│ Update   │ │ Send SMS │ │ Update   │
│ status   │ │ to user  │ │ dashboard│
└──────────┘ └──────────┘ └──────────┘

... later ...

┌──────────────────┐
│ Fetchr Webhook   │
│ POST /webhooks/  │
│ fetchr           │
│ { tracking:      │
│   FE123456789,   │
│   status:        │
│   'delivered' }  │
└─────┬────────────┘
      │
      │ Verify HMAC signature
      ▼
┌──────────────────┐       ┌──────────────┐
│ Delivery Service │──────▶│ PostgreSQL   │
│ Update status    │       │ UPDATE       │
│ Add timeline     │       │ shipment     │
└─────┬────────────┘       └──────────────┘
      │
      │ Emit event
      ▼
┌─────────────────┐
│  Kafka          │
│  delivery.      │
│  status         │
└─────┬───────────┘
      │
      ├────────────┬──────────────┐
      │            │              │
      ▼            ▼              ▼
┌──────────┐ ┌──────────┐ ┌──────────┐
│ Order    │ │ Notif.   │ │ User     │
│ Service  │ │ Service  │ │ Real-time│
│ Mark     │ │ Push     │ │ update   │
│ delivered│ │ notif.   │ │ via      │
│          │ │          │ │ Socket.io│
└──────────┘ └──────────┘ └──────────┘
```

---

## 🔐 Security Architecture

```
┌────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                          │
│                                                             │
│  1. Network Layer                                           │
│     - Cloudflare (DDoS protection, WAF)                    │
│     - SSL/TLS 1.3 (all traffic encrypted)                  │
│     - IP whitelisting for admin panel                      │
│                                                             │
│  2. API Gateway Layer                                       │
│     - JWT validation (all requests)                         │
│     - Rate limiting (100 req/min per user)                 │
│     - Request size limits (10MB)                            │
│     - API key validation (vendor/partner APIs)             │
│                                                             │
│  3. Application Layer                                       │
│     - Input validation (class-validator)                    │
│     - SQL injection prevention (Prisma ORM)                │
│     - XSS protection (sanitize inputs)                     │
│     - CSRF tokens (forms)                                  │
│     - Helmet.js (security headers)                         │
│                                                             │
│  4. Authentication & Authorization                          │
│     - JWT with short expiry (7 days)                       │
│     - Refresh tokens (30 days)                             │
│     - RBAC (Role-Based Access Control)                     │
│       * Customer                                            │
│       * Vendor                                              │
│       * Influencer                                          │
│       * Admin                                               │
│       * Super Admin                                         │
│     - MFA (SMS OTP for high-value transactions)            │
│                                                             │
│  5. Data Layer                                              │
│     - Encryption at rest (database encryption)             │
│     - Encryption in transit (TLS)                          │
│     - Sensitive field encryption (AES-256)                 │
│       * Passwords (bcrypt, 10 rounds)                      │
│       * Card tokens (PCI-compliant tokenization)           │
│     - Database access control (least privilege)            │
│                                                             │
│  6. Payment Security                                        │
│     - PCI-DSS Level 1 compliance                           │
│     - Never store card details (tokenization only)         │
│     - Stripe Elements (client-side encryption)             │
│     - 3D Secure (SCA compliance)                           │
│     - Fraud detection (Stripe Radar)                       │
│                                                             │
│  7. Monitoring & Logging                                    │
│     - Sentry (error tracking, real-time alerts)            │
│     - CloudWatch (infrastructure monitoring)               │
│     - Audit logs (all admin actions)                       │
│     - Failed login tracking (rate limit after 5)           │
│                                                             │
└────────────────────────────────────────────────────────────┘
```

---

## 📈 Scalability Architecture

```
┌────────────────────────────────────────────────────────────┐
│                    SCALING STRATEGY                         │
│                                                             │
│  Current: MVP (1K users)                                    │
│  ├─ Single EC2 instance per service                        │
│  ├─ Single database instances                              │
│  └─ Cost: ~$500/month                                      │
│                                                             │
│  Phase 1: Growth (10K users)                                │
│  ├─ Kubernetes (auto-scaling 2-5 pods per service)        │
│  ├─ Read replicas (PostgreSQL, MongoDB)                    │
│  ├─ Redis cluster (3 nodes)                                │
│  └─ Cost: ~$2,000/month                                    │
│                                                             │
│  Phase 2: Scale (100K users)                                │
│  ├─ Multi-region deployment (UAE, Saudi)                   │
│  ├─ Database sharding (by region)                          │
│  ├─ CDN for all assets (CloudFront)                        │
│  ├─ Dedicated Elasticsearch cluster (3 nodes)              │
│  └─ Cost: ~$10,000/month                                   │
│                                                             │
│  Phase 3: Enterprise (1M+ users)                            │
│  ├─ Global CDN (6+ regions)                                │
│  ├─ Database federation (by country)                       │
│  ├─ Dedicated GPU instances (AI services)                  │
│  ├─ Advanced caching (CloudFront + Redis)                  │
│  ├─ Message queue clustering (Kafka)                       │
│  └─ Cost: ~$50,000/month                                   │
│                                                             │
└────────────────────────────────────────────────────────────┘
```

---

## 🎯 Type Safety Flow

```
┌─────────────────────────────────────────────────────────────┐
│             TYPE SAFETY ACROSS PLATFORM                      │
│                                                              │
│  packages/types/src/                                         │
│  └─ product.ts                                               │
│     export interface Product { ... }                         │
│                                                              │
│          │                                                   │
│          │ Import                                            │
│          ▼                                                   │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  Backend Services (NestJS)                           │    │
│  │                                                       │    │
│  │  import { Product } from '@aromasouq/types';        │    │
│  │                                                       │    │
│  │  @Controller('products')                             │    │
│  │  export class ProductController {                    │    │
│  │    @Post()                                           │    │
│  │    create(@Body() dto: CreateProductRequest):       │    │
│  │            Promise<Product> {                        │    │
│  │      // Full type safety                            │    │
│  │    }                                                 │    │
│  │  }                                                   │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│          │                                                   │
│          │ API Response                                      │
│          ▼                                                   │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  Frontend (Next.js / React Native)                   │    │
│  │                                                       │    │
│  │  import { Product } from '@aromasouq/types';        │    │
│  │                                                       │    │
│  │  const ProductCard: React.FC<{ product: Product }>  │    │
│  │    = ({ product }) => {                              │    │
│  │      // IntelliSense: product.taxonomy.oudType      │    │
│  │      // IntelliSense: product.scent.topNotes        │    │
│  │    };                                                │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│          │                                                   │
│          │ Database Query                                    │
│          ▼                                                   │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  Database Layer (Prisma / Mongoose)                  │    │
│  │                                                       │    │
│  │  import { Product } from '@aromasouq/types';        │    │
│  │                                                       │    │
│  │  const ProductSchema = new Schema<Product>({        │    │
│  │    // Schema matches type definition exactly        │    │
│  │  });                                                 │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  ✅ Result: End-to-end type safety                          │
│     - Zero runtime type errors                              │
│     - IntelliSense everywhere                               │
│     - Refactoring-safe                                      │
│     - Self-documenting code                                 │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

**Last Updated**: November 2025
**Version**: 1.0.0
